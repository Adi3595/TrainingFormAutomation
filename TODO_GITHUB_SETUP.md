# GitHub Setup TODO

- [ ] Create root `.gitignore` (ignore node_modules, secrets, uploads, build artifacts, optional data/csv)
- [ ] Update root `README.md` with environment variable guidance (backend `.env`)
- [ ] Update `backend/package.json` to add missing `start` script
- [ ] Create `ENV_EXAMPLE.md` at root
- [ ] Ensure sensitive files (credentials) are ignored
- [ ] Initialize git repo (if not already), create branch `blackboxai/github-setup`
- [ ] Remove/ignore unused folders (csvs/, data/, backend/uploads/) as decided
- [ ] Commit changes
- [ ] Push to GitHub (after user provides repo URL or creates repo)

